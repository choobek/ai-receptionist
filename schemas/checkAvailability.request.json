{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "checkAvailability request",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "service",
    "timezone"
  ],
  "properties": {
    "requestId": {
      "type": "string",
      "description": "Optional trace identifier passed through logs and responses."
    },
    "service": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id"
      ],
      "properties": {
        "id": {
          "type": "string",
          "enum": [
            "consultation",
            "urgent_consultation",
            "implant_consultation",
            "orthodontic_consultation",
            "aesthetic_consultation",
            "hygiene"
          ],
          "description": "Supported clinic service identifier. Use consultation for a first visit or whenever the exact treatment path is still unclear."
        },
        "name": {
          "type": "string"
        },
        "durationMinutes": {
          "type": "integer",
          "minimum": 15,
          "maximum": 240,
          "default": 45
        }
      }
    },
    "requestedDate": {
      "type": "string",
      "format": "date",
      "description": "Preferred local clinic date. Optional for first_available searches; when omitted, the workflow starts from today in the clinic timezone."
    },
    "requestedTime": {
      "type": "string",
      "pattern": "^([01]\\d|2[0-3]):[0-5]\\d$",
      "description": "Optional local clinic time in 24-hour HH:MM format. Omit when unknown."
    },
    "timePreference": {
      "type": "string",
      "enum": [
        "specific_time",
        "morning",
        "afternoon",
        "evening",
        "first_available"
      ],
      "default": "first_available"
    },
    "timezone": {
      "type": "string",
      "default": "Europe/Warsaw"
    },
    "language": {
      "type": "string",
      "enum": [
        "pl",
        "en"
      ],
      "description": "Optional conversation language for speech-safe slot wording."
    },
    "locationId": {
      "type": "string",
      "default": "main-clinic"
    },
    "limit": {
      "type": "integer",
      "minimum": 1,
      "maximum": 5,
      "default": 3
    },
    "searchDays": {
      "type": "integer",
      "minimum": 1,
      "maximum": 14,
      "description": "How many consecutive clinic days to search from requestedDate. Use it for first_available and for broad multi-day morning/afternoon/evening ranges such as next week."
    },
    "patient": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "fullName": {
          "type": "string"
        },
        "phoneE164": {
          "type": "string",
          "pattern": "^\\+[1-9]\\d{7,14}$"
        },
        "isExistingPatient": {
          "type": "boolean"
        }
      }
    }
  },
  "allOf": [
    {
      "if": {
        "properties": {
          "timePreference": {
            "const": "specific_time"
          }
        },
        "required": [
          "timePreference"
        ]
      },
      "then": {
        "required": [
          "requestedTime"
        ]
      }
    },
    {
      "if": {
        "required": [
          "requestedTime"
        ]
      },
      "then": {
        "required": [
          "requestedDate"
        ]
      }
    },
    {
      "if": {
        "properties": {
          "timePreference": {
            "enum": [
              "morning",
              "afternoon",
              "evening"
            ]
          }
        },
        "required": [
          "timePreference"
        ]
      },
      "then": {
        "required": [
          "requestedDate"
        ]
      }
    }
  ],
  "examples": [
    {
      "requestId": "req_001",
      "service": {
        "id": "consultation",
        "name": "Konsultacja",
        "durationMinutes": 45
      },
      "requestedDate": "2026-03-12",
      "timePreference": "morning",
      "timezone": "Europe/Warsaw",
      "language": "pl",
      "limit": 3
    },
    {
      "requestId": "req_002",
      "service": {
        "id": "consultation",
        "name": "Konsultacja",
        "durationMinutes": 45
      },
      "timePreference": "first_available",
      "timezone": "Europe/Warsaw",
      "language": "pl",
      "limit": 3,
      "searchDays": 5
    },
    {
      "requestId": "req_003",
      "service": {
        "id": "hygiene",
        "name": "Higienizacja",
        "durationMinutes": 45
      },
      "requestedDate": "2026-03-23",
      "timePreference": "afternoon",
      "timezone": "Europe/Warsaw",
      "language": "pl",
      "limit": 3,
      "searchDays": 5
    }
  ]
}
